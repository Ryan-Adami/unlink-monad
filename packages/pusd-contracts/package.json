{
	"name": "@repo/pusd-contracts",
	"version": "0.1.0",
	"private": true,
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"require": {
				"require": "./dist/index.cjs",
				"types": "./dist/index.d.ts"
			}
		}
	},
	"scripts": {
		"build": "runx build lib index.ts -d src -f cjs --platform node",
		"check:lint": "run-eslint",
		"check:types": "run-tsc",
		"test": "run-vitest"
	},
	"dependencies": {
		"solc": "0.8.30",
		"viem": "2.46.3"
	},
	"devDependencies": {
		"@repo/tools": "workspace:*",
		"vitest": "3.2.4"
	}
}
